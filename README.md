# web-kitchen-vite

Многостраничный статический сайт (MPA) на **Vite**.

- HTML-страницы лежат в `src/` (и подпапках).
- Общие фрагменты подключаются через `@@include()` из `src/partials/`.
- Стили — **SCSS** (`src/styles/`).
- Скрипты — модульные **ESM** (`src/scripts/`).

## Быстрый старт

```bash
npm install
npm run dev
```

Откройте: `http://localhost:5173/`

## Скрипты

- `npm run dev` — режим разработки
- `npm run build` — production-сборка в `dist/`
- `npm run preview` — предпросмотр сборки


## Сборка

https://github.com/viktorygis/web-kitchen-vite-pages — пример готовой сборки для деплоя. В `dist/` уже есть все страницы, стили и скрипты. Просто загрузите `dist/` на хостинг.


## Структура

```text
.
├─ public/                # статика (img, fonts)
├─ scripts/               # vite-плагин для @@include()
├─ src/
│  ├─ index.html
│  ├─ *.html              # корневые страницы
│  ├─ */**/*.html         # страницы в подпапках
│  ├─ partials/           # НЕ страницы, а общие фрагменты
│  ├─ styles/             # SCSS
│  └─ scripts/            # JS
└─ vite.config.js
```

## @@include()

Пример для корневой страницы:

```html
@@include('partials/head.html', {"title": "Главная", "description": "..."})
```

Пример для страницы в подпапке:

```html
@@include('../partials/head.html', {"title": "Страница", "description": "..."})
```

## Деплой

- Frontend: выполните `npm run build` и загрузите содержимое `dist/` на хостинг.

## Рефакторинг

Если хотите «почистить» репозиторий, сначала определите, какие разделы/страницы реально нужны. Затем можно удалить лишние каталоги в `src/` и неиспользуемые SCSS/JS-модули.


## Технологии
- **Vite** — сборщик и дев-сервер
- **SCSS** — препроцессор для стилей
- **ESM** — модульная система для JavaScript
- **@@include()** — для подключения общих фрагментов в HTML
- **GitHub Pages** — для бесплатного хостинга статических сайтов


## Правила
✔ &lt; вместо <
✔ &gt; вместо >


## Задачи
 ### Убрать теги
 Убрать теги с `example` и обернуть <pre><code> в <div class="code-block">

### посмотреть по смысу информацию и исправить

### исправь все ошибки

### брать теги

 Убрать теги с  `grid-column-50` и `grid-column-100`

### Определение в таком виде:
<p class="note"><strong>Стрелочные функции</strong> — компактный современный синтаксис для создания функций.<br />Они особенно удобны для коротких действий и колбэков.</p>



Стандартные изменения для всех страниц:
1. HTML Структура
✅ Добавлена полная навигация: <nav class="nav-tabs" id="page-nav"> с якорями
✅ Все разделы имеют уникальные id для навигации
✅ Использованы семантические теги: <section>, <article> где уместно
2. Оформление кода
✅ Все примеры в <div class="code-block"> → <pre><code>
✅ Экранирование всех специальных символов HTML
✅ Единообразное форматирование
3. Блоки информации
✅ Определения: <p class="note"><strong>Термин</strong> — описание.</p>
✅ Примечания: <div class="note-block"> для информационных блоков
✅ Предупреждения: <div class="warning"> для критичной информации
✅ Таблицы: <table class="table-note"> для сравнений
4. Структура содержания
Введение
Основные понятия
Основной контент (специфичный для каждой страницы)
Сравнение/таблицы
Лучшие практики
Ловушки и типичные ошибки
Полезные шаблоны (где применимо)
Итоги
5. JavaScript
✅ Добавлена плавная прокрутка при клике на пункты навигации
✅ Использование closest() для делегирования событий
Удалённые элементы (общие для всех)
❌ Сложные data-атрибуты для интерактивных примеров
❌ Встроенные eval для выполнения кода (непрактично)
❌ Множественные классы оформления (grid-column-*, example, и т.д.)
❌ Неиспользуемые теги и обёртки
❌ Дублирование информации между разделами
Добавленные элементы (общие для всех)
✅ Полная навигация с якорями
✅ Таблицы сравнений
✅ Примеры кода с пошаговым разбором
✅ Раздел "Типичные ошибки" с примерами
✅ Раздел "Лучшие практики"
✅ Раздел "Полезные шаблоны"
✅ Итоговый раздел с выводами
Технические исправления
HTML парсинг:

Все символы <, > в текстовом контенте заменены на &lt;, &gt;
Все & заменены на &amp; для корректного парсинга
Консистентность:

Одинаковая структура всех страниц
Одинаковые классы для стилизации
Одинаковые форматы примеров кода
Доступность:

Все навигационные элементы имеют правильные id и href
Использованы семантические теги HTML5